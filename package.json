{
  "devDependencies": {
    "@pnpm/eslint-config": "^1.1.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "prettier": "3.1.0"
  },
  "scripts": {
    "check": "exec prettier . --check",
    "format": "exec prettier . --write",
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
